package me.ghosttypes.reaper.modules;

import me.ghosttypes.reaper.Reaper;
import meteordevelopment.meteorclient.systems.modules.Category;

/**
 * Module Loader - Category constants for Reaper modules.
 * Simplified from the original ML.java to just provide category references.
 */
public class ML {
    /**
     * Main Reaper category (combat modules).
     * In the original 1.19.4 version, this was called "Reaper" and used for combat modules.
     */
    public static final Category R = Reaper.CATEGORY;
}
